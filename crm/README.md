# CRM Celery Integration

This module adds a scheduled task that generates a weekly CRM report summarizing total customers, orders, and revenue.

## 🧰 Setup Instructions

### 1. Install Dependencies
```bash
pip install -r requirements.txt
